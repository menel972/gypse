import '.utils.dart';

class Strings {
  static String questionsCollection = PirvateStrings().questionsCollection;
  static String answersCollection = PirvateStrings().answersCollection;
  static String usersCollection = PirvateStrings().usersCollection;
}
