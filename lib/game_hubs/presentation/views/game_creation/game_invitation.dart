part of '../game_creation_screen.dart';

class GameInvitation extends StatelessWidget {
  const GameInvitation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
